build_settings:
  ignore:
    - "vendor"
    - "tests"

setup:
  sheel:
    - "echo %BUILDPATH%"
  composer:
    action: "install"

test:
  php_unit:
    config:
      - "phpunit.xml.dist"
    directory:
      - "tests/"
    coverage: "tests/logs/coverage"
  php_mess_detector:
    rules:
      - "codesize"
      - "unusedcode"
      - "naming"
    allow_failures: true
  php_code_sniffer:
    path: "src"
    standard: "PSR1,PSR2"
  php_docblock_checker:
    path: "src"
  php_cpd:
    allow_failures: true
